# FastAPI and server
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.12
aiofiles>=24.1.0

# PPTX processing
python-pptx>=1.0.2

# PDF processing
PyMuPDF>=1.24.0
pypdf>=5.1.0
pikepdf>=9.2.1
reportlab>=4.2.4
pdfplumber>=0.11.4

# Document conversions
python-docx>=1.1.2
openpyxl>=3.1.5

# Image processing
Pillow>=11.0.0
img2pdf>=0.5.1
pdf2image>=1.17.0
numpy>=1.24.0
# rembg removed - using remove.bg API instead (saves ~500MB RAM)

# HTTP requests (for CloudConvert API)
requests>=2.31.0

# Encryption/Security
cryptography>=43.0.3

# Windows COM automation (for PowerPoint on Windows - local dev only)
comtypes>=1.4.0; sys_platform == "win32"

# URL to PDF - DISABLED in lightweight mode (saves ~100MB)
# pyppeteer>=2.0.0

# AI Features (set GROQ_API_KEY to enable)
openai>=1.0.0

# Environment variables
python-dotenv>=1.0.0
